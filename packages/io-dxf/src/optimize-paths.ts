/**
 * Re-export shared import-path welding from `@cadkit/geometry`.
 * Kept for backward-compatible import paths in this package.
 */
export {
  optimizeImportPaths,
  simplifyCollinear,
  type OptimizeImportPathsOptions,
  type OptimizeImportPathsResult,
  type OptimizeImportPathsStats,
} from '@cadkit/geometry'
